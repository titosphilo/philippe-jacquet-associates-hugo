# Philippe Jacquet & Associates Hugo Site

Cloudflare Pages compatible Hugo project.

## Build

```powershell
hugo --gc --minify
```

Cloudflare Pages build command:

```text
hugo --gc --minify
```

Publish directory:

```text
public
```

Set the Cloudflare Pages environment variable `HUGO_VERSION` to the Hugo version used for deployment.
